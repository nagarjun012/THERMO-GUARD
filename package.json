{
  "name": "thermosafe-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "THERMOSAFE - AI-Powered Heat Early Warning System",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "@vercel/node": "^13.0.1"
  }
}
